{
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.9",
    "multer": "^1.4.2"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client2 && npm install && npm run build"
  }
}
