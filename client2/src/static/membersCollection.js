import ray from '../images/ray.jpg';

const membersCollection = [
    {
        title: 'Ray fdsa',
        description:
            "Ray Wong",
        waterbottleUrl: "url(" + ray + ")",
        direction: 'left',
        time: 1000,
    },
];

export default membersCollection;